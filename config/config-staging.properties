# Staging Environment Configuration
# Overrides base config.properties

environment=staging

# Staging-specific URLs
base.url.staging=https://the-internet.herokuapp.com
api.base.url=https://jsonplaceholder.typicode.com

# Staging may use headless for faster execution
headless=false
