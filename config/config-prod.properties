# Production Environment Configuration
# Overrides base config.properties

environment=prod

# Production URLs
base.url.prod=https://the-internet.herokuapp.com
api.base.url=https://jsonplaceholder.typicode.com

# Production - always headless
headless=true

# Minimal retries in production
retry.count=1
